import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.werkapp.app',
  appName: 'WerkApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
